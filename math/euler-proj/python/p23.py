from lib.math import divisors

goal = 28123
sums = 0
abundants = [i for i in range(goal) if sum(divisors(i)) > 2 * i]

print(abundants)
for n in [i for i in range(goal - 12) if i % 2 == 0 and i % 3 == 0]:
    Z
    if sum(divisors(n)) == n:
        pass
    else:
        sums += n
print(n)
